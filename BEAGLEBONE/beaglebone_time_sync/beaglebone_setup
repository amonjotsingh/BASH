#adding a default route
sudo route add default gw 192.168.7.1

#pinging teh server to check if we are having a connection or not 
ping 8.8.8.8

#adding server 8.8.8.8 to /etc/resolv.conf 
echo "nameserver 8.8.8.8" >> /etc/resolv.conf

